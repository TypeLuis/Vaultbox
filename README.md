set up

- git clone
- npm i
- npm run initialize (Sudo on Mac && Linux)
- populate env in Backend & Frontend 
- npm run dev (Sudo on Mac && Linux)




"private": true,
"workspaces": [
"Backend",
"Frontend"
],

allows you to run `npm i` and installs from boths folders